interface:
  display_name: "Adaptive Router for Codex"
  short_description: "Routes each turn to the right GPT-5.6 tier"
  default_prompt: "Use $adaptive-router-for-codex to route this task to the appropriate GPT-5.6 model."
policy:
  allow_implicit_invocation: true
